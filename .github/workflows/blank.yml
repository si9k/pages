
name: CIx
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
         docker run dorowu/ubuntu-dekstop-lxde-vnc
         sudo apt install screen -y
         docker ps -a
      
         
        
